[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --import-mode=importlib
markers =
    caddy_integration: real Uvicorn, guarded PostgreSQL, mount, and pinned Caddy tests
